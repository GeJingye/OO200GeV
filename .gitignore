# ===========================================================================
#  2021_OO200  —  .gitignore
#
#  策略 / Policy
#    * 二进制文件：全部忽略（按扩展名列出，见下）。
#    * 文本文件：全部保留（.cxx/.h/.C/.txt/.md/.json/Makefile/脚本 …）。
#
#  规则语义 / Semantics
#    * `*.ext` 不含斜杠 → 在仓库内**任意层级**递归生效（子目录同样适用）。
#    * 不含斜杠的基础名（如 Thumbs.db）同理。
#
#  说明 / Note
#    旧版用「目录级」规则忽略产物，会连带忽略目录内的文本文件。
#    本版一律改为按扩展名忽略二进制，避免误伤源码。
#    这是黑名单策略：新增二进制格式时需在对应分区补一条规则；末尾的显式白名单
#    保证文本类型永不被忽略。
#
#  本次变更 / Change
#    1) 删除 `StPicoMixedEvent/StRoot_kaif&zih/`（目录级）→ 该目录内 **121 个文本
#       文件**恢复可见（同事的 StPicoEvent / StPicoDstMaker / StElectronMcMaker /
#       StRefMultCorr_zih / StPicoMixedEvent_{kaif,zih} 等整套源码）。
#       注意：
#         · 这些文件此前从未被跟踪，本次只是让它们「可见」，不会自动加入版本库。
#         · 若你**确实**想把这份同事的 vendored 代码排除在版本库之外，请把下面
#           「可选：排除第三方 vendor 目录」一段取消注释 —— 但那与「保留所有文本
#           文件」的目标相冲突，二者只能取其一。
#    2) 删除 `roots/`（目录级）→ 由下面的 `*.root` / `*.png` 按扩展名接管。
# ===========================================================================

# ---- ROOT / 分析产物 -------------------------------------------------------
*.root
*.root.old
*.pcm
*.pch

# ---- 图片 ------------------------------------------------------------------
*.png
*.jpg
*.jpeg
*.gif
*.bmp
*.tif
*.tiff
*.webp
*.ico
*.pdf

# ---- 压缩包 / Office 文档 --------------------------------------------------
*.zip
*.tar
*.tar.gz
*.tgz
*.gz
*.bz2
*.xz
*.7z
*.rar
*.doc
*.docx
*.xls
*.xlsx
*.ppt
*.pptx

# ---- 编译产物 / 共享库 / 可执行文件 ----------------------------------------
*.o
*.a
*.obj
*.lib
*.so
*.dylib
*.dll
*.exe
*.mod

# ---- 其它二进制数据格式 ----------------------------------------------------
*.pyc
*.pyd
*.npy
*.npz
*.bin
*.h5
*.hdf5
*.parquet
*.db
*.sqlite
*.sqlite3

# ---- 操作系统 / 编辑器垃圾文件 ---------------------------------------------
.DS_Store
Thumbs.db
desktop.ini
*~

# ---- 文本永远保留（显式白名单，防止上面的规则误伤） ------------------------
!*.cxx
!*.cpp
!*.cc
!*.h
!*.hpp
!*.C
!*.txt
!*.md
!*.json
!*.yml
!*.yaml
!*.xml
!*.csv
!*.csh
!*.sh
!*.py
!*.consign
!Makefile
!*.mk
!README*
!.gitignore
!.gitattributes

# ---- 可选：排除第三方 vendor 目录（默认关闭；开启后会连带忽略其中的文本文件） --
# StPicoMixedEvent/StRoot_kaif&zih/
